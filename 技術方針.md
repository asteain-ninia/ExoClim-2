# ExoClim 技術方針

> **状態**: Phase 3 で決定。本文は未確定。

## 0. 決定方針

- 旧 ExoClim（React 19 + Vite 6 + TypeScript 5.8 + D3 + Recharts）の構成は**前提としない**。再検討から始める。
- 採用技術は要件（オフライン動作・地図描画・パラメータ操作UI・ヘッドレス出力可能性）と AI 協働開発の容易さから選定する。
- 各技術選定には「なぜそれを選んだか」「他の候補に対する優位性」を明記する。

## 1. 技術スタック

### 1.1 デスクトップ vs Web

> 未決定。論点:
> - オフライン動作の必要性（旧版は Web）
> - ファイルシステムへの直接アクセス（カスタム地図インポート・結果エクスポート）
> - 配布形態（インストーラ vs 静的サイト）

### 1.2 言語

> 未決定（TypeScript が有力だが要検討）。

### 1.3 UIフレームワーク

> 未決定。候補と論点を Phase 3 で整理。

### 1.4 状態管理

> 未決定。シミュレーションパラメータと実行状態の管理方式。

### 1.5 計算層

> 未決定。論点:
> - 純粋関数として実装するか、エージェントベースの可変状態を許容するか
> - WebWorker / WASM / Native のどこで実行するか
> - 決定性（同入力→同出力）の保証

### 1.6 描画

> 未決定。論点:
> - Canvas vs SVG vs WebGL
> - 緯度経度グリッド・ベクトル場・等値線・ストリームラインの描画

### 1.7 ビルドツール

> 1.1〜1.3 の選定に従う。

### 1.8 テスト

> 未決定。物理モデルの単体テスト（数値検証）+ E2E（UI操作）+ 比較テスト（Pasta 公開図・仮想大陸）。

## 2. アーキテクチャ

### 2.1 レイヤー分割

> Phase 3 で決定。論点:
> - シミュレーションエンジン（純粋計算）と表示層を切り離せるか
> - 各 Step（ITCZ〜気候帯）を独立モジュールとし、入出力契約だけで連結できるか
> - パラメータ・結果の単方向データフロー

### 2.2 ステップ間の値渡し

> Phase 3 で決定。旧版の問題（風帯派生 gap が UI に伝搬しない、海流ロジックが聖域化）を踏まえ、ステップ間の入力契約を明示する設計を志向する。

### 2.3 検証ハーネス

> Phase 3 で決定。論点:
> - ヘッドレス実行で再現図を出力する仕組み
> - Pasta 公開図と仮想大陸の双方を比較対象として扱う方法

## 3. 設計判断の根拠

> Phase 3 で各選定の根拠を記述する。
