# 現状.md — 未解決課題・既知不具合・残作業

> 完了した実装の履歴は `実装済み.md` を参照。
> 完了した課題は即時削除する（開発ガイド §重要な注意事項 参照）。

## テスト総数

- ユニットテスト: **2件**（sanity 2件。Vitest 2.x 上）
- 契約テスト: 未整備（Step 1 実装後に開始予定）
- E2E テスト: 未整備（Playwright は P4-5 UI 着手時に導入予定）
- 数値検証: 未整備（地球プリセットが揃う段階で開始予定）
- ビルド: `npm run build` 経路は未検証（typecheck / lint / test の 3 経路のみ確認済）

---

## 1. 既知の不具合

状態ラベル: **壊** = 機能が壊れている / **未接続** = ロジックはあるがUI未接続 / **部分** = 一部のみ実装

### 1.1 重大（データ整合性・基本機能に影響）

> 重大課題がない場合もこの見出しは維持し、`追跡無し` と明記する。

| # | 状態 | 問題 | 備考 |
| --- | --- | --- | --- |
| - | 追跡無し | 追跡中の重大不具合なし | コードベース未着手のため当然 |

### 1.2 中（機能不完全）

| # | 状態 | 問題 | 備考 |
| --- | --- | --- | --- |
| - | 追跡無し | 追跡中の中程度不具合なし | |

### 1.3 低（UX・表示）

| # | 状態 | 問題 | 備考 |
| --- | --- | --- | --- |
| - | 追跡無し | 追跡中の低優先度不具合なし | |

---

## 2. 未着手作業

### 2.1 仕様策定（Phase 1〜2）

> Phase 1〜2 完了済み。残課題なし。Phase 4 実装前の追加調査項目は §2.4 を参照。

### 2.2 技術方針策定（Phase 3）

> Phase 3 完了済み。残課題なし。

### 2.3 実装（Phase 4以降）

| # | 要件 | 優先度 | 備考 |
| --- | --- | --- | --- |
| P4-1a | ドメイン層 `Grid` / `Cell` 型と既定 1° 解像度生成器 | 高 | [要件定義書.md §4.1](要件定義書.md) |
| P4-1b | ドメイン層 `OrbitalParams` / `PlanetBodyParams` / `AtmosphereOceanParams` / `TerrainSource` / `PlanetParams` 型 + 地球プリセット定数 | 高 | [要件定義書.md §4.2](要件定義書.md) |
| P4-1c | ドメイン層 Step 結果型 7 種 + `SimulationResult` | 高 | [要件定義書.md §4.3](要件定義書.md) |
| P4-2 | 計算層 Step 1 ITCZ 純粋関数 + 単体テスト | 高 | [docs/spec/01_ITCZ.md](docs/spec/01_ITCZ.md) |
| P4-3 | ワーカー層 `pipeline.ts`（Step 1 のみ連結 + キャッシュ骨格） | 中 | [技術方針.md §2.2.3](技術方針.md) |
| P4-4 | 状態層 `params` / `results` / `ui` の 3 store + Worker bridge | 中 | [要件定義書.md §5.4](要件定義書.md) |
| P4-5a | UI 層 Canvas 2D マップビュー + ITCZ 中心線 | 中 | [技術方針.md §1.6](技術方針.md) |
| P4-5b | UI 層 `OrbitalParams` スライダー UI | 中 | [開発ガイド.md §2.2.4](開発ガイド.md) |
| P4-6〜P4-11 | Step 2〜7 を順次（風帯→海流→気流→気温→降水→気候帯） | 中 | [docs/spec/02〜07](docs/spec/) |

### 2.4 Phase 4 実装前の追加調査

> P4-prep-1 〜 P4-prep-6 はユーザー側の手作業（スクリーンショット転記・動画要約・追加 WebFetch）で 2026-04-28 に解消済み。詳細は [実装済み.md](実装済み.md) の 2026-04-28 P4-prep 解消エントリを参照。
>
> 残課題は各 docs/spec/ の §7 未確定論点に集約された設計判断（Phase 4 実装着手時に確定）。具体的な定量係数（蒸発散量計算式、長日/短日スケーリング、ラベル→定量降水量変換など）は Phase 4 実装着手時に決定する。

---

## 3. 構造的課題

| # | 深刻度 | 問題 | 備考 |
| --- | --- | --- | --- |
| - | 追跡無し | 追跡中の構造的課題なし | |

---

## 4. 旧 ExoClim（`e:/WORKSPACE/ExoClim-/`）からの移行作業

| # | 状態 | 問題 | 備考 |
| --- | --- | --- | --- |
| M1 | 未対応 | 旧リポジトリの GitHub Archive 化 | 現コードはローカルに残存。新リポを GitHub に push する段階で旧 `asteain-ninia/ExoClim-` を archive 化する |
| M2 | 未対応 | お手本画像（`tests/fixtures/idealized_continent_2.jpg`）の移送 | 検証用フィクスチャとして ExoClim-2 にコピー。機械可読化の中止判断は維持（[memory] reference_image_attempt 参照） |
| M3 | 未対応 | プロジェクトメモリの引き継ぎ | `~/.claude/projects/e--WORKSPACE-ExoClim-/memory/` から方針系メモリ（primary_reference / user_language / rebuild_plan の趣旨）を新プロジェクトメモリへ転記。実装関連メモリは破棄 |

---

## 5. 修正優先順位

2026-04-28 時点の推奨順序（Phase 1〜3 完了済み、P4-prep-1〜6 解消済み、P4-0 bootstrap 完了済み）:

1. **P4-1a 〜 P4-1c**: ドメイン層型定義（`Grid` / `Cell` / `PlanetParams` / Step 結果型）を順次。地球プリセットを併設
2. **P4-2**: Step 1 ITCZ 純粋関数 + 単体テスト
3. **P4-3 以降**: ワーカー → 状態層 → UI と層を上げていく
4. **旧リポからの移行作業**（M1〜M3）は適宜並行
