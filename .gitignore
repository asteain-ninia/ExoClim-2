# Claude Code ローカル設定（ユーザー固有）
.claude/settings.local.json

# ユーザーが docs/ に配置する一次資料の生 zip（展開版を docs/ 内ディレクトリで管理する）
docs/*.zip

# Node / Vite / Vitest
node_modules/
dist/
coverage/
.vite/
*.log
npm-debug.log*
.DS_Store

# TypeScript incremental build artifact（tsc -b 経由）
tsconfig.tsbuildinfo

# 検証ハーネスの外部資産（[技術方針.md §2.3.2]）
tests/fixtures-external/

# Playwright 出力（[開発ガイド.md §3.4]）
test-results/
playwright-report/
playwright/.cache/
